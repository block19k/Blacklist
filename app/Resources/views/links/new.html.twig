{% extends 'base.html.twig' %}

{% block body %}
    <h1>URL kūrimas</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

            <a href="{{ path('links_index') }}"  class = "btn btn-info">Grįžti į sąrašą</a>
{% endblock %}
